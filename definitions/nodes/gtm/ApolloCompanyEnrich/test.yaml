$schema: https://docs.unoverse.ai/schemas/nodes/test.schema.json

testData:
  config:
    domain: apollo.io
  inputs:
    signal: {}
  expect:
    found: "return output.found === true"
    company: "return typeof output.company.name === 'string'"
