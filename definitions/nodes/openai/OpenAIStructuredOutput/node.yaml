$schema: ../../../_schema/node.schema.json

# Transcribed from src/OpenAIStructuredOutput/node/index.ts.

type: OpenAIStructuredOutput
kind: PromiseNode

name: GPT-5 Structured Output
category: AI
color: "#2F6F66"
logoUrl: https://res.cloudinary.com/sonik/image/upload/v1749262616/gravity/icons/ChatGPT-Logo.svg.webp

description: Extract structured data using the GPT-5 Responses API with a JSON schema

whenToUse: >-
  Pick when the output must be JSON that strictly matches a schema (wire the schema and
  content inputs), schema-enforced extraction and structuring. Reserve it for when strict
  conformance matters; for free-form prose or multi-turn tool use it is the wrong,
  over-constrained choice.

capabilities:
  # An extraction is a model call, so it is non-deterministic. Re-running is correct.
  cacheable: false
