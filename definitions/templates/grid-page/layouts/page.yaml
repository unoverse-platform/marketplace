# The DETAIL arrangement (`page` state): the ONE interface the guest opened, full
# width. Its occupant fills it; the ✕ writing the state back releases it and the
# grid returns — all by name-match, no director judgment needed for the dance.
type: Box
style:
  direction: column
  width: full
children:
  - director:
      limit: 1
