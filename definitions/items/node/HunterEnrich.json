{
  "kind": "node",
  "name": "HunterEnrich",
  "pack": "gtm",
  "fingerprint": "8332ac570c84d45c",
  "title": "Hunter Enrich",
  "description": "Enrich a person or a company from an email, a LinkedIn profile or a domain",
  "category": "Go To Market",
  "icon": "https://res.cloudinary.com/sonik/image/upload/v1781366295/gravity/icons/hunter.png",
  "whenToUse": "Enrich a PERSON or a COMPANY from an email, a LinkedIn profile (handle or URL), or a domain, returning the full profile: job, employment, location, socials and company metrics. Use it for full enrichment; finding or verifying a single address, or listing a domain's emails, are different jobs.\n",
  "definition": {
    "package": {
      "name": "gtm",
      "packageFile": "$schema: ../_schema/package.schema.json\n\n# Two vendors, one package: Apollo.io for company and people data, Hunter.io for email.\n# One credential each, one base URL each, no SDKs.\n#\n# The retired TypeScript is at `_legacy/nodes/gtm/`.\n\nname: gtm\ndisplayName: Go To Market\ndescription: Company and people discovery, enrichment and email verification\nversion: 2.0.0\ncategory: productivity\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1775374180/gravity/icons/apollo.jpg\n\nfeatures:\n  - Apollo company and people search, with filters\n  - Apollo company and people enrichment\n  - Hunter email discovery, finding and verification\n\nallowedHosts:\n  - api.apollo.io\n  - api.hunter.io\n\nrequires:\n  credential: [apiKeyHeader, apiKeyQuery]\n  transport: [json]\n  paginate: [page]\n  executorVersion: \">=1.4.0\"\n",
      "credentials": {
        "apolloCredential.yaml": "$schema: ../../_schema/credential.schema.json\n\nname: apolloCredential\ndisplayName: Apollo.io\ndescription: Apollo.io master API key for people and organisation search\ndocumentationUrl: https://docs.apollo.io/docs/create-api-key\n\nproperties:\n  - name: apiKey\n    displayName: API Key\n    type: string\n    required: true\n    secret: true\n    description: Your Apollo.io master API key, required for the search endpoints\n    placeholder: \"\"\n",
        "hunterCredential.yaml": "$schema: ../../_schema/credential.schema.json\n\nname: hunterCredential\ndisplayName: Hunter.io\ndescription: Hunter.io API key for email discovery, verification and enrichment\ndocumentationUrl: https://hunter.io/api-documentation/v2\n\nproperties:\n  - name: apiKey\n    displayName: API Key\n    type: string\n    required: true\n    secret: true\n    description: Your Hunter.io API key, found under API in your Hunter dashboard\n    placeholder: \"\"\n"
      },
      "shared": {
        "apollo.yaml": "# What every Apollo call shares.\n#\n# The key travels in the `x-api-key` HEADER, which is `apiKeyHeader`, an executor\n# capability. `Cache-Control: no-cache` is sent because the retired client sent it: Apollo\n# is documented as caching search responses, and dropping the header would silently change\n# which results come back.\n\ncredential: &credential\n  scheme: apiKeyHeader\n  header: x-api-key\n  value: \"{{ credentials.apolloCredential.apiKey }}\"\n\nheaders: &headers\n  Cache-Control: no-cache\n\n# NOTE for the bodies below: every filter is a comma-separated config field turned into the\n# array Apollo wants, with\n#\n#   String(config.x || '').split(',').map(s => s.trim()).filter(Boolean)\n#\n# and an EMPTY array is left out of the body rather than sent, because Apollo reads an empty\n# filter as \"match nothing\" rather than as \"no filter\". That is why each body is built by an\n# expression that assembles only the keys that have values, instead of a static object.\n",
        "hunter.yaml": "# What every Hunter call shares.\n#\n# TWO auth styles, and that is Hunter's doing rather than ours: `/discover` takes the key in\n# an X-API-KEY header, every other endpoint takes it as an `api_key` query parameter. Both\n# are executor capabilities, so each call names the one its endpoint wants.\n\nqueryCredential: &queryCredential\n  scheme: apiKeyQuery\n  param: api_key\n  value: \"{{ credentials.hunterCredential.apiKey }}\"\n\nheaderCredential: &headerCredential\n  scheme: apiKeyHeader\n  header: X-API-KEY\n  value: \"{{ credentials.hunterCredential.apiKey }}\"\n\n# Hunter wraps every payload in `data` and reports failures in `errors`, sometimes with a\n# 200. Without this a quota error would emit an empty result and read as \"not found\".\nerror: &error\n  when: \"return !!(response.errors && response.errors.length)\"\n  message: \"return response.errors.map(e => e.details || e.id).join('; ')\"\n\n# A LinkedIn URL to the handle Hunter wants. split/join rather than a regex, because the\n# expression sandbox takes no regex literal: everything after /in/, up to a ? or #, with any\n# trailing slash removed.\nlinkedinHandle: &linkedinHandle >-\n  return String(config.linkedinHandle || '').trim()\n    .split('/in/').slice(-1)[0]\n    .split('?')[0].split('#')[0]\n    .split('/').filter(Boolean).slice(-1)[0] || ''\n"
      }
    },
    "dir": "HunterEnrich",
    "files": {
      "api/events.yaml": "# Whichever call ran. `combined` returns both halves under `data`; the single-sided endpoints\n# return one. A skipped call leaves no key, so each row falls through to null, which is the\n# honest answer when the identifier for that type was not supplied.\n\n- emit: person\n  from: response\n  value: >-\n    return ((calls.combined || {}).data || {}).person\n      || ((calls.person || {}).data || null)\n      || null\n\n- emit: company\n  from: response\n  value: >-\n    return ((calls.combined || {}).data || {}).company\n      || ((calls.company || {}).data || null)\n      || null\n",
      "api/run.yaml": "# THREE endpoints, one node, chosen by `type`. Each call is gated so exactly one runs, which\n# is why this is three entries rather than one call with a templated path: the query\n# parameters differ per endpoint too, and a templated path would still need the branch.\n#\n# Person enrichment prefers a LinkedIn handle over an email when both are given, matching\n# the retired code.\n\n- name: company\n  when: \"return config.type === 'company' && !!String(config.domain || '').trim()\"\n  method: GET\n  url: https://api.hunter.io/v2/companies/find\n  credential: { $ref: hunter#queryCredential }\n  query:\n    domain: \"return String(config.domain || '').trim()\"\n  transport: json\n  error: { $ref: hunter#error }\n\n- name: person\n  when: >-\n    return config.type === 'person'\n      && !!(String(config.linkedinHandle || '').trim() || String(config.email || '').trim())\n  method: GET\n  url: https://api.hunter.io/v2/people/find\n  credential: { $ref: hunter#queryCredential }\n  query:\n    linkedin_handle: { $ref: hunter#linkedinHandle }\n    email: >-\n      return String(config.linkedinHandle || '').trim() ? '' : String(config.email || '').trim()\n  transport: json\n  error: { $ref: hunter#error }\n\n- name: combined\n  when: >-\n    return (config.type || 'combined') === 'combined' && !!String(config.email || '').trim()\n  method: GET\n  url: https://api.hunter.io/v2/combined/find\n  credential: { $ref: hunter#queryCredential }\n  query:\n    email: \"return String(config.email || '').trim()\"\n  transport: json\n  error: { $ref: hunter#error }\n",
      "config.yaml": "$schema: ../../../_schema/config.schema.json\n\nconfigSchema:\n  type: object\n  required: [type]\n  properties:\n    type:\n      type: string\n      title: Enrichment Type\n      description: >-\n        combined returns both from an email. person takes an email or a LinkedIn profile.\n        company takes a domain.\n      enum: [combined, person, company]\n      enumNames: [Combined, Person, Company]\n      default: combined\n    email:\n      type: string\n      title: Email\n      default: \"\"\n      \"ui:field\": template\n      \"ui:dependencies\": { type: [combined, person] }\n    linkedinHandle:\n      type: string\n      title: LinkedIn Handle or URL\n      description: A handle, or a full profile URL. The URL is reduced to the handle for you.\n      default: \"\"\n      \"ui:field\": template\n      \"ui:dependencies\": { type: [person] }\n    domain:\n      type: string\n      title: Domain\n      default: \"\"\n      \"ui:field\": template\n      \"ui:dependencies\": { type: [company] }\n\n    # WHO MAY RUN THIS BOX, set by whoever builds the workflow (15-who-can-run-it.md).\n    #\n    # INSTANCE config, not the manifest's `auth`, and the two answer different questions.\n    # The manifest is the node author's FLOOR: \"this node is inherently privileged\". These\n    # are the builder's, per box on the canvas, because the same node type legitimately\n    # faces staff on one workflow and customers on another.\n    #\n    # A role especially has to live here: `finance:approve` is a claim THIS deployment's\n    # identity provider mints, and a node published to the marketplace cannot know the role\n    # vocabulary of every universe that installs it.\n    #\n    # The executor takes the STRICTER of the two. Neither can loosen the other.\n    authRequired:\n      type: boolean\n      title: Require sign-in\n      description: Only signed-in callers can run this step.\n      default: false\n      \"ui:widget\": toggle\n    authRole:\n      type: string\n      title: Require role\n      description: A claim the caller must carry, like finance:approve.\n      default: \"\"\n      \"ui:dependencies\": { authRequired: true }\n\n\"ui:order\": [authRequired, authRole, type, email, linkedinHandle, domain]\n",
      "interface.yaml": "$schema: ../../../_schema/interface.schema.json\n\ninputs:\n  - name: signal\n    type: object\n    description: Trigger. The identifier usually templates from upstream output.\n\noutputs:\n  - name: person\n    type: object\n    description: The person profile, or null when this run enriched a company or found nothing\n  - name: company\n    type: object\n    description: The company profile, or null when this run enriched a person or found nothing\n\ncredentials:\n  - name: hunterCredential\n    required: true\n    displayName: Hunter.io\n",
      "node.yaml": "$schema: ../../../_schema/node.schema.json\n\ntype: HunterEnrich\nkind: PromiseNode\nname: Hunter Enrich\ncategory: Go To Market\ncolor: \"#FF5500\"\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1781366295/gravity/icons/hunter.png\ndescription: Enrich a person or a company from an email, a LinkedIn profile or a domain\nwhenToUse: >\n  Enrich a PERSON or a COMPANY from an email, a LinkedIn profile (handle or URL), or a\n  domain, returning the full profile: job, employment, location, socials and company\n  metrics. Use it for full enrichment; finding or verifying a single address, or listing a\n  domain's emails, are different jobs.\n\n# WHO MAY RUN THIS (DECLARATIVE_NODES.md §9.13). Compulsory on every node, so that a node\n# nobody thought about cannot look the same as one deliberately left open.\n#\n# `required: false` adds no requirement of its own: the node runs for whoever the trigger\n# admitted. The trigger instance's publicEntry toggle still owns the door, and requirements\n# only ever narrow.\nauth:\n  required: false\n\ncapabilities:\n  cacheable: true\n\n# One vendor. The package allows Apollo too, for its other four nodes.\nallowedHosts: [api.hunter.io]\n",
      "test.yaml": "$schema: ../../../_schema/test.schema.json\n\ntestData:\n  config:\n    type: company\n    email: \"\"\n    linkedinHandle: \"\"\n    domain: close.io\n  inputs:\n    signal: {}\n  expect:\n    company: \"return output.company !== null && typeof output.company === 'object'\"\n    person: \"return output.person === null\"\n"
    }
  }
}
