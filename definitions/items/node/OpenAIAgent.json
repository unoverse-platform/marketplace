{
  "kind": "node",
  "name": "OpenAIAgent",
  "pack": "openai",
  "fingerprint": "3f443d46c9cbb940",
  "title": "OpenAI Agent",
  "description": "Multi-turn agent that works through a problem using MCP tools",
  "category": "AI",
  "icon": "https://res.cloudinary.com/sonik/image/upload/v1749262616/gravity/icons/ChatGPT-Logo.svg.webp",
  "whenToUse": "Reach for this when a step needs the model to work through a problem using tools rather than answer in one pass: look something up, act on what it found, then decide what to do next. A single prompt with no tool use is cheaper and more predictable done elsewhere. Attach MCP provider nodes by service edge to give it capabilities.",
  "detail": {
    "inputs": [
      {
        "name": "signal",
        "type": "object",
        "description": "Data from previous nodes that can be referenced in templates"
      }
    ],
    "outputs": [
      {
        "name": "thinking",
        "type": "string",
        "description": "Short customer-safe status lines, emitted in real time"
      },
      {
        "name": "stream",
        "type": "object",
        "description": "Streaming text response, emitted in real time"
      },
      {
        "name": "reasoning",
        "type": "string",
        "description": "The model's own reasoning summary (not the narrator's status line)"
      },
      {
        "name": "mcpResult",
        "type": "object",
        "description": "MCP tool results"
      },
      {
        "name": "text",
        "type": "string",
        "description": "The complete generated text (final output)"
      }
    ],
    "credentials": [
      {
        "name": "openAICredential",
        "required": true,
        "displayName": "OpenAI API"
      }
    ],
    "configSchema": {
      "type": "object",
      "required": [
        "model",
        "maxTurns",
        "prompt"
      ],
      "properties": {
        "model": {
          "type": "string",
          "title": "Model",
          "enum": [
            "gpt-5.6",
            "gpt-5.6-terra",
            "gpt-5.6-luna"
          ],
          "enumNames": [
            "Sol (Frontier, complex work)",
            "Terra (Balanced intelligence/cost)",
            "Luna (Cost-optimized, high-volume)"
          ],
          "default": "gpt-5.6-terra",
          "resolve": "modelTier"
        },
        "maxTurns": {
          "type": "number",
          "title": "Max Turns",
          "description": "How many times the model may go round the tool loop. 1 answers in a single pass and never calls a tool. 5 to 10 suits an agent that looks things up and acts on what it finds. Higher costs more and takes longer.",
          "default": 10,
          "minimum": 1,
          "maximum": 30
        },
        "reasoningEffort": {
          "type": "string",
          "title": "Reasoning Effort",
          "enum": [
            "none",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "medium"
        },
        "reasoningSummary": {
          "type": "string",
          "title": "Reasoning Summary",
          "description": "Summaries must be requested or the API emits no reasoning events at all. auto matches the legacy node and gives the fullest live reasoning stream; concise summaries are often too short to stream visibly.",
          "enum": [
            "auto",
            "concise",
            "detailed"
          ],
          "default": "auto"
        },
        "verbosity": {
          "type": "string",
          "title": "Verbosity",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "medium"
        },
        "systemPrompt": {
          "type": "string",
          "title": "System Prompt",
          "description": "Standing instructions for the agent: its role, how to work, when to use its tools. Optional. Shared blocks work here, e.g. {{prompt.reactAgent}}.",
          "default": "",
          "ui:field": "template"
        },
        "prompt": {
          "type": "string",
          "title": "Prompt",
          "description": "The task to work on. Usually wired in from an upstream node.",
          "default": "",
          "ui:field": "template"
        },
        "maxTokens": {
          "type": "number",
          "title": "Max Output Tokens",
          "default": 4096,
          "minimum": 1,
          "maximum": 128000
        },
        "enableConversationMemory": {
          "type": "boolean",
          "title": "Conversation Memory",
          "description": "Continue the same conversation across runs, so the agent remembers what was said earlier in this chat. Off starts fresh each run.",
          "default": false,
          "ui:widget": "toggle"
        },
        "enableUserMemory": {
          "type": "boolean",
          "title": "User Memory",
          "description": "Remember the user across conversations — the agent saves what it learns and can look it up later.",
          "default": false,
          "ui:widget": "toggle"
        },
        "enableAgentMemory": {
          "type": "boolean",
          "title": "Agent Memory",
          "description": "Give the agent a working notebook for long-running tasks — it tracks its goal, progress and next steps between runs.",
          "default": false,
          "ui:widget": "toggle"
        },
        "authRequired": {
          "type": "boolean",
          "title": "Require sign-in",
          "description": "Only a signed-in caller may run this step. Leave off and it runs for whoever the workflow's trigger admitted, which is the usual answer.",
          "default": false,
          "ui:widget": "toggle"
        },
        "authRole": {
          "type": "string",
          "title": "Require role",
          "description": "A claim the caller's account must carry, as noun:verb (finance:approve, payments:refund). Leave blank to require only that they are signed in.",
          "default": "",
          "ui:dependencies": {
            "authRequired": true
          }
        }
      }
    }
  },
  "definition": {
    "package": {
      "name": "openai",
      "packageFile": "$schema: ../_schema/package.schema.json\n\n# The package envelope, and now the ONLY envelope: this package is fully\n# declarative. All five nodes are manifests, there is no src/ and no package.json,\n# so nothing here is built, published or imported. The retired TypeScript sits at\n# _legacy/nodes/openai for reference.\n#\n# A code node of the same type always beats a manifest at load, which is what made\n# migrating one node at a time possible. With the code gone there is nothing left\n# to beat these.\n\nname: openai\ndisplayName: OpenAI\ndescription: OpenAI integration for Unoverse\nversion: 1.3.0\ncategory: ai\n# First-party. The marketplace shows this on every node the package registers, so\n# \"who am I trusting with this credential\" is answerable without leaving the screen.\npublisher: unoverse\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1749262616/gravity/icons/ChatGPT-Logo.svg.webp\n\nfeatures:\n  - GPT-5.6 models\n  - Live two-way voice, with the model interruptible mid-sentence\n  - Streaming responses\n  - Function/tool calling\n  - Text embeddings\n  - Structured JSON output\n  - Vision (image inputs)\n  - Realtime voice calls with transcripts of both sides\n\n# The ONLY hosts this package's nodes may call. The executor refuses anything else.\n#\n# A manifest cannot execute code, but without this it could still say \"send this\n# credential to evil.example\", which is exfiltration with no code at all. One short\n# reviewable list is what makes a manifest safe to accept from someone else.\nallowedHosts:\n  - api.openai.com\n\n# Executor capabilities every manifest node here depends on. Deploy REFUSES this\n# package if the target universe's executor cannot satisfy them, so a manifest can\n# never name a capability that only exists on a newer executor.\n# `ws` and the raised floor came with OpenAIRealtimeVoice (2026-08-12): a realtime session is a\n# SOCKET, and the duplex runtime that holds one open landed in executor 1.5.0.\nrequires:\n  credential: [bearer]\n  transport: [sse, json, ws]\n  # OpenAIAgent keeps its OpenAI conversation id between runs. A universe whose executor\n  # cannot do this would otherwise read a permanently cold cache and start a new\n  # conversation every turn: memory that silently never remembers.\n  state: [read, merge]\n  executorVersion: \">=1.5.0\"\n",
      "credentials": {
        "openAICredential.yaml": "$schema: ../../_schema/credential.schema.json\n\n# The SHAPE of the credential, never its value. Transcribed from\n# plugin-base/src/credentials/openai.ts, which this package re-exports today.\n#\n# Values live where they already live: encrypted in Postgres on a deployed universe,\n# and in the developer's own .env locally. That is why a manifest is safe to commit.\n#\n# Nodes reference this by NAME. At run time context.credentials holds EVERY credential\n# in the workflow keyed by name, and the executor must select by name, never by\n# scanning for the first object with an apiKey: openAICredential, apolloCredential,\n# hunterCredential and searchapiCredential all expose one, so a scan silently\n# authenticates against the wrong service the moment a second is added to the\n# workflow (04-credentials.md).\n\nname: openAICredential\ndisplayName: OpenAI\ndescription: Credentials for OpenAI API\ndocumentationUrl: https://platform.openai.com/api-keys\n\nproperties:\n  - name: apiKey\n    displayName: API Key\n    type: string\n    required: true\n    secret: true\n    description: Your OpenAI API key\n    placeholder: sk-...\n\n  - name: organizationId\n    displayName: Organization ID\n    type: string\n    required: false\n    secret: false\n    description: Your OpenAI organization ID (optional)\n    placeholder: org-...\n\n  - name: baseUrl\n    displayName: Base URL\n    type: string\n    required: false\n    secret: false\n    description: Custom API endpoint (optional)\n    default: https://api.openai.com/v1\n"
      },
      "shared": {
        "endpoints.yaml": "# OpenAI's endpoints. Written once here rather than seven times across the nodes.\n#\n# No chat/completions: every node uses the Responses API. `messages` is the legacy shape.\n#\n#   url: { $ref: ../../shared/endpoints.yaml#/responses }\n\nresponses: https://api.openai.com/v1/responses\nembeddings: https://api.openai.com/v1/embeddings\n\n# A CONVERSATION is OpenAI's own server-side memory: a durable id you mint once and then\n# name on every response. Items are prepended to the request and the turn's input and\n# output are appended back, so history never travels from here and is never stored here.\n# Minted with POST and an empty body; the reply carries the id.\n#\n# NO CALLER SINCE 2026-08-23. OpenAIAgent was the only one and now chains with\n# previous_response_id instead. Kept because the URL is a fact about the vendor rather\n# than about any node, and re-deriving it is the sort of small cost that argues for\n# leaving a one-line constant alone. Nothing here decides which mechanism a node uses —\n# each declares that next to its own reason (shared/tools.yaml).\nconversations: https://api.openai.com/v1/conversations\n",
        "models.yaml": "# The canonical GPT model list, the SINGLE source every OpenAI node references,\n# organised by TIER. Transcribed from src/shared/models.ts.\n#\n# Models come in three tiers. A new generation is a one-line edit per tier here, and\n# every node plus every saved workflow moves over automatically.\n#\n# The DATA lives here. The FALLBACK LOGIC (resolveModel: a retired id resolves to the\n# current best model in its tier, so replacing a generation cannot break a saved\n# workflow) is computation, not description, so it belongs to the executor. It is\n# declared per config field as `resolver: modelTier`, not written here.\n\n# One current model per tier. Update these three lines when a new generation ships.\nbyTier:\n  small: gpt-5.6-luna # cost-optimized, high-volume\n  medium: gpt-5.6-terra # balanced intelligence/cost, the default\n  large: gpt-5.6 # frontier, complex work\n\n# Balanced middle tier, the sensible default for most work.\ndefault: gpt-5.6-terra\n\n# The NARRATOR: the model that writes an agent's short \"thinking\" status line. A separate\n# concern from the model doing the work, so it is named separately.\n#\n# Deliberately NOT the 5.6 family: 5.6 is luna/sol/terra with no nano tier, and this call\n# exists to reach the user fast, not to be clever. Measured 2026-07-26, three runs each,\n# same prompt and schema:\n#\n#   gpt-5.4-nano   avg 1137ms   [1470, 905, 1035]\n#   gpt-5.6-luna   avg 2043ms   [1846, 2337, 1945]\n#\n# Luna is ~80% slower and less consistent for lines that are indistinguishable in quality.\n# Re-measure when a faster small model ships; never assume.\nnarrator: gpt-5.4-nano\n\n\n# The picker's options. Listed large first so the strongest reads first.\nenum:\n  - gpt-5.6\n  - gpt-5.6-terra\n  - gpt-5.6-luna\n\nenumNames:\n  - Sol (Frontier, complex work)\n  - Terra (Balanced intelligence/cost)\n  - Luna (Cost-optimized, high-volume)\n",
        "realtimeTools.yaml": "# The OpenAI REALTIME tool protocol.\n#\n# Vendor-specific, so it does not belong in the platform runtime. Node-agnostic, so it does not\n# belong copied into a node either. It belongs here, exactly as `openai/shared/tools.yaml` holds the\n# Responses protocol — imported whole:\n#\n#   toolExchange:\n#     $ref: ../../shared/tools.yaml\n#\n# WHY THIS IS NOT `openai/shared/tools.yaml`. Same vendor, different API, and three real differences\n# rather than cosmetic ones:\n#\n#   the call event   Responses says `response.output_item.done` and needs a `when` to tell a function\n#                    call from any other completed item. Realtime has a dedicated\n#                    `response.function_call_arguments.done`, so no `when` is needed.\n#   offer, not      Responses puts the tool list in each REQUEST, so a list that GREW since the\n#   toolsInto       last turn is re-sent for free and there is nothing to declare. A socket sends\n#                   it once at the handshake, so growth has to be announced on purpose — which is\n#                   what `offer` below is. Same fact, two shapes: `toolsInto` names a request\n#                   field, `offer` names a whole message.\n#   no continuity    Responses chains turns with previous_response_id. THE SOCKET IS THE CONTINUITY:\n#                    the conversation never ended, so there is nothing to chain.\n#\n# Also absent, and deliberately: `maxTurns` and `stuckAfterRepeats`. Both count turns, and a turn is\n# one model call. Nothing to count here. What bounds a runaway session is the executor's own idle and\n# duration ceiling.\n\n# How ONE tool is offered. `tool` is { name, description, parameters }.\ntool: >-\n  return { type: 'function', name: tool.name, description: tool.description,\n           parameters: tool.parameters }\n\n# HOW THE LIST IS RE-OFFERED once it has grown. `tools` is the whole live list, already in the\n# envelope `tool` above produces.\n#\n# ONLY `tools`: session.update MERGES (verified live 2026-08-12), so instructions, modalities,\n# voice and turn detection survive untouched. Re-sending the whole session block would be a second\n# copy of the handshake, free to drift. Safe at any moment — the vendor gates `response.create` on\n# an idle conversation, never session.update.\noffer: >-\n  return { type: 'session.update', session: { type: 'realtime', tools: tools } }\n\n# HOW THE INSTRUCTIONS ARE RE-SENT when a region skill comes into force or is cleared.\n#\n# The same shape and the same reason as `offer` above, for the other thing the handshake sent\n# once. A request/response vendor rebuilds its prompt every turn, so a skill absorbed on one\n# turn is in the next request for free and there is nothing to declare. A socket sent\n# `instructions` at the handshake and would otherwise never mention it again, so the model\n# would keep working to a prompt that no longer describes where the conversation is.\n#\n# ONLY `instructions`: session.update MERGES, so voice, modalities, tools and turn detection\n# survive untouched. `skillsText` is the platform's rendering of what is in force — it decides\n# WHICH skills apply, this decides where the vendor puts them.\n#\n# The config prompt is repeated rather than sent alone, because a merge REPLACES the field it\n# names: sending only the skills would drop the role the node was configured with.\nreinstruct: >-\n  return { type: 'session.update',\n           session: { type: 'realtime',\n                      instructions: config.systemPrompt + skillsText } }\n\n# How a call ARRIVES. `match` is what makes a stream tractable: events keep coming for the whole\n# conversation, and this says which of them is a tool call rather than a transcript or audio.\ncall:\n  match: response.function_call_arguments.done\n  id: \"return response.call_id\"\n  name: \"return response.name\"\n  # A JSON STRING, which the executor parses. The trap the schema warns about: passed through\n  # unparsed, the tool receives a string where it expected its parameters.\n  arguments: \"return response.arguments\"\n\n# How the result goes BACK. TWO messages, because OpenAI separates them: the output joins the\n# conversation, then a response is asked for. Without the second the model holds the result silently.\nresult: >-\n  return [\n    { type: 'conversation.item.create',\n      item: { type: 'function_call_output', call_id: call.id, output: call.output } },\n    { type: 'response.create' }\n  ]\n",
        "tools.yaml": "# The OpenAI Responses tool protocol.\n#\n# Vendor-specific, so it does NOT belong in the platform runtime. Node-agnostic, so it\n# does not belong copied into every node either. It belongs here: shared by every OpenAI\n# node, imported whole.\n#\n#   toolExchange:\n#     $ref: ../../shared/tools.yaml\n#     maxTurns: 5            # local keys layer on top and win\n#\n# When OpenAI changes how a function call is expressed, exactly one file changes.\n# NO SDK. This is the wire format, described as data.\n\n# How ONE tool is offered to the model. `tool` is { name, description, parameters }.\ntool: >-\n  return { type: 'function', name: tool.name, description: tool.description,\n           parameters: tool.parameters }\ntoolsInto: tools\n\n# How a call ARRIVES in the stream.\ncall:\n  match: response.output_item.done\n  when: \"return response.item.type === 'function_call'\"\n  id: \"return response.item.call_id\"\n  name: \"return response.item.name\"\n  arguments: \"return response.item.arguments\"\n\n# How ONE result goes BACK. `call` is { id, name, output }.\nresult: >-\n  return { type: 'function_call_output', call_id: call.id, output: call.output }\nresultsInto: input\n\n# Always auto: which tool to reach for is carried by the tool DESCRIPTIONS, never by\n# forcing a choice.\nchoiceInto: tool_choice\nchoice: auto\n\n# NO `continuity` HERE, and that is deliberate. How a turn reaches the one before it is a\n# per-node CHOICE on this vendor, not one wire format, because OpenAI offers two and\n# REFUSES both at once:\n#\n#   previous_response_id   chains response to response. A new id every turn, known only\n#                          after the turn ends. Declared by OpenAIStream, and since\n#                          2026-08-23 by OpenAIAgent too — which now outlives a single\n#                          execution by REMEMBERING that id (continuity.remember) rather\n#                          than by holding a durable one.\n#   conversation           a durable id, minted once and named on every turn, with the API\n#                          keeping the history. No caller today.\n#\n# Sending both is `mutually_exclusive_parameters`, a hard 400 (confirmed against the live\n# API 2026-08-12). So a node that names a conversation must NOT inherit continuity, and\n# this file cannot decide for both. Each declares its own, next to the reason it chose it.\n"
      }
    },
    "dir": "OpenAIAgent",
    "files": {
      "api/events.yaml": "# EVERYTHING THAT LEAVES THIS NODE, one row per output connector, in the SAME ORDER the\n# connectors are declared in interface.yaml. Read this and you know the node's whole\n# outward behaviour without opening another file. Lint holds the order.\n#\n# `from` says where a row fires:\n#   response   an event on the main SSE stream, matched by type\n#   narrator   a line from the cheap side model\n#   tool       after a tool call RETURNS, with its result\n#   complete   once, at the end\n\n# The customer-safe status line, written FOR the customer by the narrator. Not the\n# model's own thinking, which is `reasoning` below and is not safe to show.\n- emit: thinking\n  from: narrator\n  value: \"return narrator.line\"\n\n# Accumulated, not the fragment: a consumer wants the answer so far, not one word. And\n# throttled, or a long answer becomes hundreds of events. Nothing held back is lost; it\n# is flushed when the run ends.\n- emit: stream\n  from: response\n  match: response.output_text.delta\n  value: \"return response.delta\"\n  accumulate: true\n  throttleMs: 200\n\n# The MODEL's own reasoning summary. Only produced when reasoningEffort is above none\n# AND a reasoningSummary is requested; the API sends nothing otherwise. The part-done\n# marker contributes the paragraph break between summary sections — without it the\n# sections concatenate (\"**Ensuring X****Balancing Y**\"), a legacy behaviour lost in\n# the first migration.\n#\n# Throttled by TIME, not characters. The legacy 150-char threshold held a summary\n# SHORTER than 150 chars until the end-of-run flush — whole turns produced no live\n# reasoning and it all arrived as one late lump (observed 2026-08-06). A time throttle\n# emits every delta within 200ms whatever its size, which is what makes the stream\n# lane feel live, and is the cadence this connector had in the legacy node's heyday.\n- emit: reasoning\n  from: response\n  match: [response.reasoning_summary_text.delta, response.reasoning_summary_part.done]\n  value: 'return response.type === \"response.reasoning_summary_part.done\" ? \"\\n\\n\" : response.delta'\n  accumulate: true\n  throttleMs: 200\n\n# The tool RESULT. It is never in the HTTP stream: our tool loop produced it, which is\n# why this cannot be a `from: response` row. Emitting the tool CALL here instead was a\n# real bug, since it fired before the tool had run and carried the model's raw arguments.\n- emit: mcpResult\n  from: tool\n  value: \"return { name: call.name, result: call.output }\"\n\n# The settled answer: the last accumulated value the stream reached. `at(-1)` and\n  # not `pop()`: the expression sandbox allows no mutating method, by design.\n- emit: text\n  from: complete\n  value: \"return events.filter(e => e.emit === 'stream').map(e => e.value).at(-1) ?? ''\"\n",
      "api/narrate.yaml": "# The narrator: the `thinking` line, written by a second and much cheaper model in THIS\n# agent's voice. Deliberately not shared, because the copy is tuned for this node.\n#\n# Not to be confused with `reasoning` in events.yaml, which is the MAIN model's own\n# summary. The platform contributes only timing: a local line at 0ms because the round\n# trip has a ~1s floor and the main model's first token beats it, never awaited, fall\n# back on any failure, and drop late lines.\n#\n# Which connector its line lands on is NOT here: that is the `from: narrator` row in\n# events.yaml, like every other thing that leaves this node.\n\ninstant:\n  - Let me look into this for you...\n  - Okay, one moment while I check...\n  - Let me see how I can help...\n  - Just a second while I look at this...\n  - Checking the best way to help you...\n\nfallback: Checking the best next step...\n\nrequest:\n  method: POST\n  url: { $ref: endpoints#responses }\n  credential:\n    scheme: bearer\n    token: \"{{ credentials.openAICredential.apiKey }}\"\n  timeoutMs: 8000\n  transport: json\n  body:\n    # NOT the model doing the work: this line has to beat the main stream, so it wants\n    # the fastest small model there is. Named in shared/models.yaml with the timings\n    # behind the choice, so every agent moves together when a faster one ships.\n    model: { $ref: models#narrator }\n    max_output_tokens: 600\n    instructions: |-\n      You are writing the short status line a virtual assistant shows the customer while it prepares its reply — the visible \"thinking\" line above the answer.\n\n      The line must:\n      - Be one sentence only, 6–12 words.\n      - Sound like the assistant getting on with the task: what it is checking, confirming, or looking into.\n      - Use natural, warm, present-tense assistant voice.\n      - NEVER be commentary about the customer's message itself (never \"what X refers to\", \"the message says\", \"you mentioned\").\n      - NEVER quote or repeat the customer's words.\n      - NEVER repeat abbreviations, acronyms, brand names, or product names from the message.\n      - Avoid assumptions about the customer's goal, preference, eligibility, or situation.\n      - Avoid phrases like \"the message is brief\", \"I asked\", \"I need to understand\", or \"before answering\".\n      - No private chain-of-thought, no step-by-step reasoning.\n      - Never mention tool, system, or service names.\n      - No label such as \"Reasoning summary:\".\n\n      Good examples:\n      - Okay, let me check the right support route.\n      - Let me clarify what help you need today.\n      - I'll confirm the relevant options for your request.\n      - Checking the best next step for this query.\n      - Let me review the available support options.\n      - I'll check the right process to follow.\n\n      Bad examples:\n      - Confirming what \"hello\" refers to for your query.\n      - The message is very brief, so I asked a few questions.\n      - I am reasoning about what the user might want.\n      - The user may be looking for a course or qualification.\n      - I need to infer their goal before answering.\n      - Reasoning summary: I checked the available options.\n    # WHAT THE NARRATOR IS TOLD about this moment. Two moments, two framings, chosen with\n    # an ordinary {{#if}} over the {{event.*}} root, exactly like any other template value\n    # in any other manifest field. Nothing here is special.\n    input: |-\n      {{#if (eq event.kind \"turnStart\")}}\n      For topical context only (do NOT quote or reference it), the message from the customer was: \"\"\"{{event.userMessage}}\"\"\". Write the status line the assistant shows while preparing its reply.\n      {{else}}\n      The assistant is now looking something up for the customer using an internal capability named \"{{event.toolName}}\"{{#if event.args}} with input {{toJSON event.args}}{{/if}}. Write the thinking line describing what is being checked or done (never name the capability).\n      {{/if}}\n    text:\n      format:\n        type: json_schema\n        name: thinking\n        strict: true\n        schema:\n          type: object\n          additionalProperties: false\n          required: [line]\n          properties:\n            line: { type: string }\n\nresponse:\n  line: >-\n    return JSON.parse(\n      response.output.filter(o => o.type === 'message')\n        .flatMap(o => o.content).filter(c => c.type === 'output_text')\n        .map(c => c.text).join('') || '{}'\n    ).line\n",
      "api/run.yaml": "# The main call: OpenAI's Responses API. NO SDK.\n#\n# The Agents SDK is client-side orchestration over this same endpoint, so what it gives\n# you is a loop, not a capability. Doing the loop from here keeps an agent portable: the\n# platform counts turns and spots a stuck one, and everything about OpenAI's wire format\n# lives in this folder.\n#\n# A CALL is one thing: how it is made AND how its reply is framed. That is why transport,\n# terminator and error live here and not in a file of their own.\n\n# ── CONVERSATION MEMORY ──────────────────────────────────────────────────────────────\n#\n# ONE step here, and it is the READ. The write is the tool loop's, declared as\n# `continuity.remember` in toolExchange.yaml, because the id this feature stores does not\n# exist until the model has finished speaking. See that file for why the two halves live\n# apart.\n#\n# What is NOT here is the point. No history is read, assembled, injected or stored. The\n# only thing that crosses this boundary is an opaque id; OpenAI holds the turns against\n# it and prepends them itself. So this stays a memory feature with no memory in it.\n#\n# KEYED BY THE CHAT THREAD, not the user, because a signed-out guest HAS a thread and has\n# no user id. Keying on an absent user id would resolve every guest to one key and feed\n# them each other's history, which is a leak, not a degraded feature. The trade is that\n# memory lives as long as the thread: a new thread starts fresh.\n\n- name: cached\n  # Switched off, or no thread id: either way nothing is read and the run starts a fresh\n  # chain. Skipped leaves NO `calls.cached` at all, which is what `generate` tests.\n  when: \"return !!config.enableConversationMemory && !!scope.conversationId\"\n  state: read\n  key: \"openai:conversation:{{ scope.conversationId }}\"\n\n- name: generate\n  method: POST\n  url: { $ref: endpoints#responses }\n\n  credential:\n    scheme: bearer\n    token: \"{{ credentials.openAICredential.apiKey }}\"\n\n  body:\n    model: \"{{ config.model }}\"\n    input: \"{{ config.prompt }}\"\n    # THE SEED, and only on turn one. Cold (memory off, no thread, or a chain older than\n    # OpenAI's 30 days) renders empty and is dropped, which starts a new chain rather than\n    # failing — the same outcome as a page reload, and the reason no fallback is written\n    # here. From turn two the loop supplies this key itself from the id the last turn\n    # settled on, and the loop's value wins.\n    previous_response_id: \"{{ calls.cached.id }}\"\n    # THE ROLE, THEN WHATEVER APPLIES WHERE THE CONVERSATION IS STANDING.\n    #\n    # `skillsText` is the loop's, rendered from the skills attached to the region this\n    # conversation's last search anchored in. Empty until one applies, so an agent that never\n    # reaches an attached region sends exactly what it always did.\n    #\n    # ROLE FIRST is not cosmetic: `instructions` is the cached prefix, so the stable text has\n    # to come before the part that changes or a skill loading would throw away the whole\n    # cached prefix instead of just its tail.\n    instructions: \"{{ config.systemPrompt }}{{ skillsText }}\"\n    max_output_tokens: \"{{ config.maxTokens }}\"\n    stream: true\n    reasoning:\n      effort: \"{{ config.reasoningEffort }}\"\n      summary: \"{{ config.reasoningSummary }}\"\n    text:\n      verbosity: \"{{ config.verbosity }}\"\n\n  timeoutMs: 600000\n\n  retry:\n    attempts: 3\n    backoff: exponential\n    # 520/522/524 are CLOUDFLARE EDGE errors, not OpenAI ones: the origin returned something the\n    # edge could not read, never answered, or exceeded the edge timeout. They are transient in exactly\n    # the way 502/503/504 are, and a live run hit a 520 after 154s with no retry because they were\n    # missing here. Added alongside the origin 5xx rather than instead of them.\n    on: [429, 500, 502, 503, 504, 520, 522, 524]\n\n  # How the reply is framed.\n  transport: sse\n  terminator: \"[DONE]\"\n\n  # A 200 carrying an error body must not read as success.\n  error:\n    when: \"return response.type === 'response.failed'\"\n    message: \"return response.response.error.message\"\n",
      "api/toolExchange.yaml": "# The tool loop's protocol, described HERE and not in the platform. The runtime only\n# counts turns, spots a stuck loop, and mints what discovery unlocks.\n\n# OpenAI's tool protocol, imported whole. Local keys below layer on top.\n$ref: tools\n\n# HOW A TURN REACHES THE ONE BEFORE IT. Declared here rather than in shared/tools.yaml\n# because the vendor offers two mechanisms and refuses both together (see that file).\n#\n# THIS NODE USED THE CONVERSATIONS API UNTIL 2026-08-23 and now chains response to\n# response. The two are not interchangeable, and the difference is one of ORDER: a\n# conversation id exists BEFORE the call, so the old code minted one, named it, and wrote\n# it to Redis in three steps that all ran ahead of the model. A response id only exists\n# AFTER the call, which is why `remember` below is the loop's job and not a step in\n# run.yaml.\n#\n# What it costs: a response is kept for 30 days (a conversation object has no TTL), so a\n# thread left alone for longer resumes cold instead of resuming. `run.yaml` handles that\n# by simply not naming an id it does not have — the seed is absent and the run starts\n# fresh, which is the same thing that happens on a page reload.\ncontinuity:\n  match: response.completed\n  from: \"return response.response.id\"\n  into: previous_response_id\n  # KEYED BY THE THREAD, exactly as the conversation id was, and for the same reason: the\n  # key has to be stable across turns or each run writes somewhere the next never looks.\n  # `conversationId` is the thread (one per page load, shared by every call in it) — NOT\n  # `chatId`, which is minted per turn to address components and would give every turn its\n  # own dead key.\n  remember:\n    when: \"return !!config.enableConversationMemory && !!scope.conversationId\"\n    key: \"openai:conversation:{{ scope.conversationId }}\"\n\n# SKILLS IN FORCE, held against the CONVERSATION for the same reason the chain id is: a new\n# user message is a new execution, and a skill that lived only in memory would be re-applied\n# from nothing on every message. Keyed on `conversationId` (the thread), never `chatId`\n# (minted per turn) — a per-turn key would hold a set nothing ever reads back.\nskills:\n  remember:\n    key: \"openai:skills:{{ scope.conversationId }}\"\n\n# The dial. 1 answers in a single pass and cannot use a tool at all; higher makes it\n# work through a problem.\nmaxTurns: \"{{ config.maxTurns }}\"\n# The SAME call with the SAME arguments three times is stuck. maxTurns does not catch\n# it: a model calling many DIFFERENT tools forever is not stuck by that rule.\nstuckAfterRepeats: 3\n",
      "config.yaml": "$schema: ../../../_schema/config.schema.json\n\nconfigSchema:\n  type: object\n  # `prompt` IS REQUIRED, for the reason spelled out in OpenAI/config.yaml: this node's body builds\n  # the Responses API's required `input` from `prompt` alone, and `resolveBody` omits a key that\n  # resolved to \"\" — so an empty prompt drops the field and the vendor answers\n  # `400 Missing required parameter: 'input'` rather than anything a reader could act on.\n  required: [model, maxTurns, prompt]\n\n  properties:\n    model:\n      type: string\n      title: Model\n      enum: { $ref: models#enum }\n      enumNames: { $ref: models#enumNames }\n      default: { $ref: models#default }\n      resolve: modelTier\n\n    # THE DIAL THAT MAKES THIS AN AGENT.\n    #\n    # One turn is one model call. A tool call costs another, because the result has to go\n    # back for the model to use it. So maxTurns: 1 is a one-shot generator that cannot use\n    # tools at all, and anything above it is an agent working through a problem.\n    #\n    # That is the only real difference between this and a streaming text node, which is\n    # why they are one node with a dial rather than two nodes.\n    maxTurns:\n      type: number\n      title: Max Turns\n      description: >-\n        How many times the model may go round the tool loop. 1 answers in a single pass\n        and never calls a tool. 5 to 10 suits an agent that looks things up and acts on\n        what it finds. Higher costs more and takes longer.\n      default: 10\n      minimum: 1\n      maximum: 30\n\n    reasoningEffort:\n      type: string\n      title: Reasoning Effort\n      enum: [none, low, medium, high, xhigh, max]\n      default: medium\n\n    reasoningSummary:\n      type: string\n      title: Reasoning Summary\n      description: >-\n        Summaries must be requested or the API emits no reasoning events at all. auto\n        matches the legacy node and gives the fullest live reasoning stream; concise\n        summaries are often too short to stream visibly.\n      enum: [auto, concise, detailed]\n      default: auto\n\n    verbosity:\n      type: string\n      title: Verbosity\n      enum: [low, medium, high]\n      default: medium\n\n    systemPrompt:\n      type: string\n      title: System Prompt\n      description: >-\n        Standing instructions for the agent: its role, how to work, when to use its tools.\n        Optional. Shared blocks work here, e.g. {{prompt.reactAgent}}.\n      default: \"\"\n      ui:field: template\n\n    prompt:\n      type: string\n      title: Prompt\n      description: The task to work on. Usually wired in from an upstream node.\n      default: \"\"\n      ui:field: template\n\n    maxTokens:\n      type: number\n      title: Max Output Tokens\n      default: 4096\n      minimum: 1\n      maximum: 128000\n\n    # THE MEMORY TOGGLES, transcribed from the retired node — they vanished in the migration\n    # and the whole memory system sat idle because nothing declared them (found live\n    # 2026-07-30: healthy memory server, empty ingest stream). Three consumers key off\n    # these flags, and none is this manifest's business to implement:\n    #   - the ENGINE injects the goal-memory toolset from `enableAgentMemory`\n    #     (PlatformMemoryCore reads the node's config — no node-side code, ever)\n    #   - the EXECUTOR hydrates the prompt with the user's context snapshot before the run\n    #   - the EXECUTOR ingests each completed turn to the memory server after it\n    # THE CONVERSATION SWITCH. Off, so a run starts from nothing unless someone asks for\n    # continuity — the same default the two memory toggles below carry, and the one that\n    # makes a test reproducible.\n    #\n    # OFF DOES NOT MEAN NO CONVERSATION. The node still mints one per run (api/run.yaml):\n    # it is how turns chain inside the tool loop now that this node names a conversation\n    # instead of a previous_response_id. What the toggle gates is whether that id is\n    # REMEMBERED, so off is a fresh conversation every run rather than no conversation.\n    enableConversationMemory:\n      type: boolean\n      title: Conversation Memory\n      description: >-\n        Continue the same conversation across runs, so the agent remembers what was said\n        earlier in this chat. Off starts fresh each run.\n      default: false\n      \"ui:widget\": toggle\n\n    enableUserMemory:\n      type: boolean\n      title: User Memory\n      description: >-\n        Remember the user across conversations — the agent saves what it learns and can look\n        it up later.\n      default: false\n      \"ui:widget\": toggle\n    enableAgentMemory:\n      type: boolean\n      title: Agent Memory\n      description: >-\n        Give the agent a working notebook for long-running tasks — it tracks its goal,\n        progress and next steps between runs.\n      default: false\n      \"ui:widget\": toggle\nui:order:\n  [model, maxTurns, reasoningEffort, reasoningSummary, verbosity, systemPrompt, prompt, maxTokens, enableConversationMemory, enableUserMemory, enableAgentMemory]\n",
      "interface.yaml": "$schema: ../../../_schema/interface.schema.json\n\ninputs:\n  - name: signal\n    type: object\n    description: Data from previous nodes that can be referenced in templates\n\noutputs:\n  # The NARRATOR's line, not the model's. `reasoning` below is the model's own summary;\n  # this is the short customer-safe status written by a separate cheap model.\n  - name: thinking\n    type: string\n    description: Short customer-safe status lines, emitted in real time\n\n  - name: stream\n    type: object\n    description: Streaming text response, emitted in real time\n\n  - name: reasoning\n    type: string\n    description: The model's own reasoning summary (not the narrator's status line)\n\n  - name: mcpResult\n    type: object\n    description: MCP tool results\n\n  - name: text\n    type: string\n    description: The complete generated text (final output)\n\ncredentials:\n  - name: openAICredential\n    required: true\n    displayName: OpenAI API\n\nserviceConnectors:\n  # Attach MCP provider nodes here to give the agent capabilities. Nothing wired means\n  # no tools, and it degrades to a single request rather than failing.\n  - name: mcpService\n    serviceType: mcp\n    isService: false\n    description: MCP service connector, automatic schema discovery\n",
      "node.yaml": "$schema: ../../../_schema/node.schema.json\n\n# The agent, as a manifest. Responses API + the tool loop. NO SDK.\n#\n# AT PARITY with the retired TypeScript agent as of 2026-08-12. Memory (both toggles +\n# lanes) restored 2026-07-30, the narrator is here (api/narrate.yaml), and cross-execution\n# conversation closed the last gap. See the note at the bottom.\n#\n# It takes the real type, because a type is the identity a saved graph stores and an\n# invented suffix would put an implementation detail on a name people read. There is no\n# TypeScript registration left to compete with it; the retired code is kept for\n# reference at _legacy/nodes/openai.\n\ntype: OpenAIAgent\nkind: CallbackNode\n\nname: OpenAI Agent\ncategory: AI\ncolor: \"#2F6F66\"\n\ndescription: Multi-turn agent that works through a problem using MCP tools\n\n# The wiring sentence used to sit in the MIDDLE. Doc 14: mechanism last, always, because\n# opening or interrupting with plumbing vocabulary drags the embedding towards wiring\n# concepts and away from the job a planner's task query actually describes.\nwhenToUse: >-\n  Reach for this when a step needs the model to work through a problem using tools rather\n  than answer in one pass: look something up, act on what it found, then decide what to do\n  next. A single prompt with no tool use is cheaper and more predictable done elsewhere.\n  Attach MCP provider nodes by service edge to give it capabilities.\n\n# WHO MAY RUN THIS (DECLARATIVE_NODES.md §9.13). Compulsory on every node, so that a node\n# nobody thought about cannot look the same as one deliberately left open.\n#\n# `required: false` adds no requirement of its own: the node runs for whoever the trigger\n# admitted. The trigger instance's publicEntry toggle still owns the door, and requirements\n# only ever narrow.\nauth:\n  required: false\n\ncapabilities:\n  # NEVER cacheable: an agent turn is non-deterministic and reaches the outside world\n  # through its tools. Reusing a prior answer would skip real side effects.\n  cacheable: false\n\n# ── CROSS-EXECUTION CONVERSATION, closed 2026-08-12 ──────────────────────────────────\n#\n# The retired node kept a previous_response_id in Redis and replayed it. This one keeps\n# OpenAI's own CONVERSATION id instead (api/run.yaml), which is the same feature reached\n# the better way round: a previous_response_id is a new value every turn, minted only\n# AFTER the turn ends, and the streaming model call is this node's last call with nothing\n# running after it. A conversation id is known BEFORE the call and never changes, so the\n# whole thing is three ordinary entries in the call list and no platform change.\n#\n# Keyed by chat thread, not by user, because a guest has a thread and no user id. Still\n# open, deliberately, and neither is needed for the feature to work:\n#\n#   reset       the retired node's \"reset conversation\" phrase. Today a thread's memory\n#               runs until the key is cleared; there is no expiry on it.\n#   identity    memory follows the THREAD. Following a signed-in user across threads and\n#               workflows needs the key to fall back between user and thread, which a\n#               state key cannot express (it renders, it does not branch).\n#\n# memory CLOSED 2026-07-30: the toggles are back in config.yaml and the two lanes\n# (hydratePromptWithUserMemory before the run, ingestConversationTurn after) run in the\n# EXECUTOR off those flags (executor/memory.ts) — the same generic wiring the engine uses\n# to inject the goal-memory toolset from enableAgentMemory. This file declares only the\n# toggles; any agent manifest that declares them gets the lanes.\n",
      "test.yaml": "$schema: ../../../_schema/test.schema.json\n\n# No MCP provider is wired on the bench, so this exercises the one-shot path\n# (maxTurns still bounds it; with no tools the loop degrades to a single request).\n# The tool loop itself is proven separately against a stub bridge.\ntestData:\n  config:\n    model: gpt-5.6\n    maxTurns: 5\n    reasoningEffort: none\n    reasoningSummary: concise\n    verbosity: low\n    systemPrompt: You are a concise research assistant.\n    prompt: Name the three states of matter, comma separated.\n    maxTokens: 300\n\n  inputs:\n    signal:\n      topic: states of matter\n\n  expect:\n    text: \"return output.text.length > 0\"\n"
    }
  }
}
