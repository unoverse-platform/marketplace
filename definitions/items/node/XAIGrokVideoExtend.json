{
  "kind": "node",
  "name": "XAIGrokVideoExtend",
  "pack": "xai-grok",
  "fingerprint": "394848be7bc0d41f",
  "title": "Grok Video Extend",
  "description": "Continue an existing video with the next few seconds of motion",
  "category": "Media & Design",
  "icon": "https://res.cloudinary.com/sonik/image/upload/v1777865512/gravity/icons/X_logo.jpg",
  "whenToUse": "Continue a video that already exists: describe what happens next and the clip grows by two to ten seconds, picking up exactly where it left off. This is how a story longer than one shot gets made — chain it after each render for staged scenes a single generation compresses too much. Making a new video from scratch or from a still image is a different job. Takes the video URL an earlier video step produced.",
  "detail": {
    "inputs": [
      {
        "name": "signal",
        "type": "object",
        "description": "Data from previous nodes, referenceable in the prompt and video url",
        "required": false
      }
    ],
    "outputs": [
      {
        "name": "videoUrl",
        "type": "string",
        "description": "The extended video, as a hosted URL"
      },
      {
        "name": "video",
        "type": "object",
        "description": "The full result: url, duration (seconds), respectModeration"
      },
      {
        "name": "metadata",
        "type": "object",
        "description": "model, generatedAt"
      }
    ],
    "credentials": [
      {
        "name": "xaiCredential",
        "required": true,
        "displayName": "xAI"
      }
    ],
    "configSchema": {
      "type": "object",
      "required": [
        "videoUrl",
        "prompt"
      ],
      "properties": {
        "videoUrl": {
          "type": "string",
          "title": "Video URL",
          "description": "The clip to continue — the extension picks up from its final frame. Wire in the videoUrl an earlier video step produced, or paste any public MP4 URL. The vendor rejects clips longer than 15 seconds, so an already-extended clip usually needs trimming before it can be extended again.",
          "default": "",
          "ui:field": "template"
        },
        "prompt": {
          "type": "string",
          "title": "What Happens Next",
          "description": "The next beat of the story — motion, camera and mood for the seconds being added, not a re-description of what already happened.",
          "default": "",
          "ui:field": "template",
          "ui:ai": {
            "editable": true
          }
        },
        "duration": {
          "type": "number",
          "title": "Extension (seconds)",
          "description": "How many seconds are added to the clip. Longer costs proportionally more.",
          "default": 6,
          "minimum": 2,
          "maximum": 10
        },
        "authRequired": {
          "type": "boolean",
          "title": "Require sign-in",
          "description": "Only a signed-in caller may run this step. Leave off and it runs for whoever the workflow's trigger admitted, which is the usual answer.",
          "default": false,
          "ui:widget": "toggle"
        },
        "authRole": {
          "type": "string",
          "title": "Require role",
          "description": "A claim the caller's account must carry, as noun:verb (finance:approve, payments:refund). Leave blank to require only that they are signed in.",
          "default": "",
          "ui:dependencies": {
            "authRequired": true
          }
        }
      }
    }
  },
  "definition": {
    "package": {
      "name": "xai-grok",
      "packageFile": "$schema: ../_schema/package.schema.json\n\n# XAI GROK, fully declarative.\n#\n# The node is a REALTIME VOICE session, not a text agent — `wss://api.x.ai/v1/realtime` — so what\n# unblocked it was `transport: ws` and the audio lane, built for openai-realtime. Its protocol is\n# identical to OpenAI's realtime format event for event, which is why shared/tools.yaml reads the same.\n#\n# TWO SOCKETS ARE INVOLVED and only one is here: the vendor socket is this manifest's `run`, while the\n# platform's audio socket to the browser is api/audio.yaml. That second lane exists only because MCP\n# cannot carry binary audio; everything else this node emits reaches the client over MCP streaming.\n\nname: xai-grok\ndisplayName: xAI Grok\ndescription: Voice conversations, image generation and video generation with xAI's Grok\n# 2.1.0: grok-voice-think-fast-2.0 (model picker + reasoning dial + gated transcription), plus the\n# transcript-accumulation and Q/A-log fixes proven live on the sibling realtime voice node.\n# 2.2.0: Grok Imagine joins the package — XAIGrokImage (imagine-image-2.0, synchronous) and\n# XAIGrokVideo (imagine-video-1.5, an async job the poll capability waits on).\n# 2.3.0: XAIGrokVideoExtend — chained shots via /v1/videos/extensions, because one 15-second\n# generation compresses a construction-to-function arc too far (probed live 2026-08-15).\nversion: 2.3.0\ncategory: ai\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1777865512/gravity/icons/X_logo.jpg\n\nfeatures:\n  - Realtime voice conversation with Grok, with MCP tool calling\n  - Image generation with Grok Imagine, typography-strong, hosted URLs back\n  - Short video generation from a prompt or a still image, with sound\n  - Video extension, continuing an existing clip shot by shot\n\nallowedHosts:\n  - api.x.ai\n\nrequires:\n  executorVersion: \">=1.6.0\"\n  credential: [bearer]\n  # ws carries the voice session; json carries the Imagine REST calls.\n  transport: [ws, json]\n  # The video node waits on a render job. Without `poll` it would settle on the job RECEIPT\n  # and emit that as the answer, with nothing anywhere reporting a problem.\n  poll: true\n",
      "credentials": {
        "xaiCredential.yaml": "$schema: ../../_schema/credential.schema.json\n\nname: xaiCredential\ndisplayName: xAI\ndescription: Credentials for xAI Grok API\ndocumentationUrl: https://console.x.ai\n\nproperties:\n  - name: apiKey\n    displayName: API Key\n    type: string\n    required: true\n    secret: true\n    description: Your xAI API key\n    placeholder: xai-\n"
      },
      "shared": {
        "tools.yaml": "# The xAI Grok REALTIME tool protocol.\n#\n# Vendor-specific, so it does not belong in the platform runtime. Node-agnostic, so it does not\n# belong copied into a node either. It belongs here, exactly as `openai/shared/tools.yaml` holds the\n# Responses protocol — imported whole:\n#\n#   toolExchange:\n#     $ref: ../../shared/tools.yaml\n#\n# WHY THIS IS NOT `openai/shared/tools.yaml`. Same vendor, different API, and three real differences\n# rather than cosmetic ones:\n#\n#   the call event   Responses says `response.output_item.done` and needs a `when` to tell a function\n#                    call from any other completed item. Realtime has a dedicated\n#                    `response.function_call_arguments.done`, so no `when` is needed.\n#   no toolsInto     Responses puts the tool list in each REQUEST. A realtime session declares it\n#                    ONCE in the handshake, so there is no request field to name; run.yaml reads\n#                    `services.mcpService.tools` into session.update itself.\n#   no continuity    Responses chains turns with previous_response_id. THE SOCKET IS THE CONTINUITY:\n#                    the conversation never ended, so there is nothing to chain.\n#\n# Also absent, and deliberately: `maxTurns` and `stuckAfterRepeats`. Both count turns, and a turn is\n# one model call. Nothing to count here. What bounds a runaway session is the executor's own idle and\n# duration ceiling.\n\n# How ONE tool is offered. `tool` is { name, description, parameters }.\ntool: >-\n  return { type: 'function', name: tool.name, description: tool.description,\n           parameters: tool.parameters }\n\n# HOW THE LIST IS RE-OFFERED once it has grown mid-call. A socket sends its tools once at the\n# handshake, so growth has to be announced on purpose — this message is that announcement.\n#\n# ONLY `tools` in the session block: session.update MERGES rather than replaces (verified live on\n# OpenAI 2026-08-12; xAI documents the same session.update semantics), so instructions, voice and\n# turn detection survive untouched. Re-sending the whole session would be a second copy of the\n# handshake, free to drift. No `type: realtime` key — xAI's session object does not carry one.\noffer: >-\n  return { type: 'session.update', session: { tools: tools } }\n\n# How a call ARRIVES. `match` is what makes a stream tractable: events keep coming for the whole\n# conversation, and this says which of them is a tool call rather than a transcript or audio.\ncall:\n  match: response.function_call_arguments.done\n  id: \"return response.call_id\"\n  name: \"return response.name\"\n  # A JSON STRING, which the executor parses. The trap the schema warns about: passed through\n  # unparsed, the tool receives a string where it expected its parameters.\n  arguments: \"return response.arguments\"\n\n# How the result goes BACK. TWO messages, because OpenAI separates them: the output joins the\n# conversation, then a response is asked for. Without the second the model holds the result silently.\nresult: >-\n  return [\n    { type: 'conversation.item.create',\n      item: { type: 'function_call_output', call_id: call.id, output: call.output } },\n    { type: 'response.create' }\n  ]\n"
      }
    },
    "dir": "XAIGrokVideoExtend",
    "files": {
      "api/events.yaml": "# One row per output connector, in interface.yaml order — the generation node's rows, because\n# the poll endpoint is the same and the outputs are deliberately interchangeable.\n#\n# `calls.extend` is the FINAL poll payload (status done), not the start receipt.\n\n- emit: videoUrl\n  from: response\n  value: \"return ((calls.extend || {}).video || {}).url || ''\"\n\n- emit: video\n  from: response\n  value: >-\n    return {\n      url: ((calls.extend || {}).video || {}).url || '',\n      duration: ((calls.extend || {}).video || {}).duration || 0,\n      respectModeration: ((calls.extend || {}).video || {}).respect_moderation === true\n    }\n\n# `generatedAt` is epoch milliseconds: the sandbox exposes Date.now() and refuses `new`.\n- emit: metadata\n  from: response\n  value: >-\n    return {\n      model: (calls.extend || {}).model || 'grok-imagine-video',\n      generatedAt: Date.now()\n    }\n",
      "api/run.yaml": "# One JOB: start the extension, poll until done — the same shape as the generation node,\n# against the same status endpoint.\n#\n# NO `model` KEY, and that is a decision, not an omission. The field is optional here and the\n# vendor's own extensions example uses the floating alias; which pinned ids the endpoint\n# accepts is undocumented, so sending `grok-imagine-video-1.5` risks failing every call for\n# consistency's sake. The generation node pins because its model list IS documented; this\n# node takes the vendor default until the docs name ids for extensions.\n#\n# NO `output.upload_url`. The reference's field table calls it required, but the page's own\n# request example omits it, and the poll endpoint is the same GET /v1/videos/{request_id}\n# that hands generations a hosted video.url. This manifest follows the example; if the\n# vendor truly enforces the field, the POST fails with a 4xx naming it — loud, first run,\n# and the moment to decide where uploads should land.\n\n- name: extend\n  method: POST\n  url: https://api.x.ai/v1/videos/extensions\n  credential:\n    scheme: bearer\n    token: \"{{ credentials.xaiCredential.apiKey }}\"\n  headers:\n    Content-Type: application/json\n  transport: json\n  timeoutMs: 120000\n  retry:\n    attempts: 3\n    backoff: exponential\n    on: [429, 500, 502, 503, 504]\n\n  body:\n    prompt: \"{{ config.prompt }}\"\n    video: >-\n      return { url: String(config.videoUrl || '').trim() }\n    duration: \"return config.duration || 6\"\n\n  # 5s x 240 = twenty minutes, the generation node's bound: an extension renders the same\n  # kind of work, and a bound set for the fast case fails exactly the slow one.\n  poll:\n    url: \"return 'https://api.x.ai/v1/videos/' + response.request_id\"\n    until: \"return response.status === 'done'\"\n    failed: \"return response.status === 'failed'\"\n    message: \"return 'Video extension failed. ' + JSON.stringify(response.error || response)\"\n    intervalMs: 5000\n    maxAttempts: 240\n",
      "config.yaml": "$schema: ../../../_schema/config.schema.json\n\nconfigSchema:\n  type: object\n  required: [videoUrl, prompt]\n  properties:\n    videoUrl:\n      type: string\n      title: Video URL\n      description: >-\n        The clip to continue — the extension picks up from its final frame. Wire in the\n        videoUrl an earlier video step produced, or paste any public MP4 URL. The vendor\n        rejects clips longer than 15 seconds, so an already-extended clip usually needs\n        trimming before it can be extended again.\n      default: \"\"\n      \"ui:field\": template\n    prompt:\n      type: string\n      title: What Happens Next\n      description: >-\n        The next beat of the story — motion, camera and mood for the seconds being added,\n        not a re-description of what already happened.\n      default: \"\"\n      \"ui:field\": template\n      \"ui:ai\":\n        editable: true\n    duration:\n      type: number\n      title: Extension (seconds)\n      description: How many seconds are added to the clip. Longer costs proportionally more.\n      default: 6\n      minimum: 2\n      maximum: 10\n\"ui:order\": [videoUrl, prompt, duration]\n",
      "interface.yaml": "$schema: ../../../_schema/interface.schema.json\n\ninputs:\n  - name: signal\n    type: object\n    description: Data from previous nodes, referenceable in the prompt and video url\n    required: false\n\n# THE SAME THREE OUTPUTS AS THE GENERATION NODE, deliberately: an extension chain reads\n# `videoUrl` off whichever step came last, so the two node types have to be interchangeable\n# to whatever consumes them.\noutputs:\n  - name: videoUrl\n    type: string\n    description: The extended video, as a hosted URL\n  - name: video\n    type: object\n    description: \"The full result: url, duration (seconds), respectModeration\"\n  - name: metadata\n    type: object\n    description: model, generatedAt\n\ncredentials:\n  - name: xaiCredential\n    required: true\n    displayName: xAI\n",
      "node.yaml": "$schema: ../../../_schema/node.schema.json\n\ntype: XAIGrokVideoExtend\n# PromiseNode WITH `poll`, exactly like the generation node: the POST returns a job receipt\n# and GET /v1/videos/{request_id} — the same status endpoint generations poll — reports the\n# finished clip.\nkind: PromiseNode\n\nname: Grok Video Extend\ncategory: Media & Design\ncolor: \"#000000\"\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1777865512/gravity/icons/X_logo.jpg\n\ndescription: Continue an existing video with the next few seconds of motion\n\nwhenToUse: >-\n  Continue a video that already exists: describe what happens next and the clip grows by two\n  to ten seconds, picking up exactly where it left off. This is how a story longer than one\n  shot gets made — chain it after each render for staged scenes a single generation\n  compresses too much. Making a new video from scratch or from a still image is a different\n  job. Takes the video URL an earlier video step produced.\n\n# WHO MAY RUN THIS (who-can-run-it.md). Compulsory on every node.\nauth:\n  required: false\n\ncapabilities:\n  # NOT cacheable: generation is non-deterministic, two runs are meant to differ.\n  cacheable: false\n",
      "test.yaml": "$schema: ../../../_schema/test.schema.json\n\n# The source clip is a genuinely anonymous public URL AND ten seconds long — both learned\n# live (2026-08-15): Google's gtv-videos-bucket 403s a server-side fetch, and the vendor\n# rejects any input over 15 seconds (\"got 52.2s\" for the Sintel trailer). Two seconds of\n# extension — the cheapest honest run. Asserts the SHAPE: a hosted URL and a plausible\n# duration, never anything about the scene.\n\ntestData:\n  config:\n    videoUrl: https://test-videos.co.uk/vids/bigbuckbunny/mp4/h264/360/Big_Buck_Bunny_360_10s_1MB.mp4\n    prompt: The camera slowly pulls back and the scene fades toward dusk\n    duration: 2\n  inputs:\n    signal: {}\n  expect:\n    videoUrl: \"return output.videoUrl.startsWith('https://')\"\n    video: >-\n      return output.video.url === output.videoUrl\n        && output.video.duration > 0\n    metadata: \"return output.metadata.generatedAt > 0\"\n"
    }
  }
}
