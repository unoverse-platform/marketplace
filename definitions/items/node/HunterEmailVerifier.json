{
  "kind": "node",
  "name": "HunterEmailVerifier",
  "pack": "gtm",
  "fingerprint": "aa5f0ebb3f8d5512",
  "title": "Hunter Email Verifier",
  "description": "Check whether one email address is real and deliverable",
  "category": "Go To Market",
  "icon": "https://res.cloudinary.com/sonik/image/upload/v1781366295/gravity/icons/hunter.png",
  "whenToUse": "Pick to CHECK that a single email address is real and deliverable before sending. Use it to verify an address you already have; discovering an unknown address is a separate, upstream step.\n",
  "detail": {
    "inputs": [
      {
        "name": "signal",
        "type": "object",
        "description": "Trigger. The email usually templates from upstream output."
      }
    ],
    "outputs": [
      {
        "name": "email",
        "type": "string",
        "description": "The address that was verified"
      },
      {
        "name": "status",
        "type": "string",
        "description": "valid, invalid, accept_all, webmail, disposable or unknown"
      },
      {
        "name": "result",
        "type": "string",
        "description": "deliverable, undeliverable or risky"
      },
      {
        "name": "score",
        "type": "number",
        "description": "Confidence 0-100"
      },
      {
        "name": "accept_all",
        "type": "boolean",
        "description": "True when the domain accepts every address, which makes the result less certain"
      },
      {
        "name": "disposable",
        "type": "boolean",
        "description": "True when the address is from a disposable provider"
      }
    ],
    "credentials": [
      {
        "name": "hunterCredential",
        "required": true,
        "displayName": "Hunter.io"
      }
    ],
    "configSchema": {
      "type": "object",
      "required": [
        "email"
      ],
      "properties": {
        "email": {
          "type": "string",
          "title": "Email",
          "description": "The address to verify",
          "default": "",
          "ui:field": "template"
        },
        "authRequired": {
          "type": "boolean",
          "title": "Require sign-in",
          "description": "Only a signed-in caller may run this step. Leave off and it runs for whoever the workflow's trigger admitted, which is the usual answer.",
          "default": false,
          "ui:widget": "toggle"
        },
        "authRole": {
          "type": "string",
          "title": "Require role",
          "description": "A claim the caller's account must carry, as noun:verb (finance:approve, payments:refund). Leave blank to require only that they are signed in.",
          "default": "",
          "ui:dependencies": {
            "authRequired": true
          }
        }
      }
    }
  },
  "definition": {
    "package": {
      "name": "gtm",
      "packageFile": "# Two vendors, one package: Apollo.io for company and people data, Hunter.io for email.\n# One credential each, one base URL each, no SDKs.\n#\n# The retired TypeScript is at `_legacy/nodes/gtm/`.\n\nname: gtm\ndisplayName: Go To Market\ndescription: Company and people discovery, enrichment and email verification\nversion: 2.0.0\ncategory: productivity\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1775374180/gravity/icons/apollo.jpg\n\nfeatures:\n  - Apollo company and people search, with filters\n  - Apollo company and people enrichment\n  - Hunter email discovery, finding and verification\n\nallowedHosts:\n  - api.apollo.io\n  - api.hunter.io\n\nrequires:\n  credential: [apiKeyHeader, apiKeyQuery]\n  transport: [json]\n  paginate: [page]\n  executorVersion: \">=1.4.0\"\n",
      "credentials": {
        "apolloCredential.yaml": "name: apolloCredential\ndisplayName: Apollo.io\ndescription: Apollo.io master API key for people and organisation search\ndocumentationUrl: https://docs.apollo.io/docs/create-api-key\n\nproperties:\n  - name: apiKey\n    displayName: API Key\n    type: string\n    required: true\n    secret: true\n    description: Your Apollo.io master API key, required for the search endpoints\n    placeholder: \"\"\n",
        "hunterCredential.yaml": "name: hunterCredential\ndisplayName: Hunter.io\ndescription: Hunter.io API key for email discovery, verification and enrichment\ndocumentationUrl: https://hunter.io/api-documentation/v2\n\nproperties:\n  - name: apiKey\n    displayName: API Key\n    type: string\n    required: true\n    secret: true\n    description: Your Hunter.io API key, found under API in your Hunter dashboard\n    placeholder: \"\"\n"
      },
      "shared": {
        "apollo.yaml": "# What every Apollo call shares.\n#\n# The key travels in the `x-api-key` HEADER, which is `apiKeyHeader`, an executor\n# capability. `Cache-Control: no-cache` is sent because the retired client sent it: Apollo\n# is documented as caching search responses, and dropping the header would silently change\n# which results come back.\n\ncredential: &credential\n  scheme: apiKeyHeader\n  header: x-api-key\n  value: \"{{ credentials.apolloCredential.apiKey }}\"\n\nheaders: &headers\n  Cache-Control: no-cache\n\n# NOTE for the bodies below: every filter is a comma-separated config field turned into the\n# array Apollo wants, with\n#\n#   String(config.x || '').split(',').map(s => s.trim()).filter(Boolean)\n#\n# and an EMPTY array is left out of the body rather than sent, because Apollo reads an empty\n# filter as \"match nothing\" rather than as \"no filter\". That is why each body is built by an\n# expression that assembles only the keys that have values, instead of a static object.\n",
        "hunter.yaml": "# What every Hunter call shares.\n#\n# TWO auth styles, and that is Hunter's doing rather than ours: `/discover` takes the key in\n# an X-API-KEY header, every other endpoint takes it as an `api_key` query parameter. Both\n# are executor capabilities, so each call names the one its endpoint wants.\n\nqueryCredential: &queryCredential\n  scheme: apiKeyQuery\n  param: api_key\n  value: \"{{ credentials.hunterCredential.apiKey }}\"\n\nheaderCredential: &headerCredential\n  scheme: apiKeyHeader\n  header: X-API-KEY\n  value: \"{{ credentials.hunterCredential.apiKey }}\"\n\n# Hunter wraps every payload in `data` and reports failures in `errors`, sometimes with a\n# 200. Without this a quota error would emit an empty result and read as \"not found\".\nerror: &error\n  when: \"return !!(response.errors && response.errors.length)\"\n  message: \"return response.errors.map(e => e.details || e.id).join('; ')\"\n\n# A LinkedIn URL to the handle Hunter wants. split/join rather than a regex, because the\n# expression sandbox takes no regex literal: everything after /in/, up to a ? or #, with any\n# trailing slash removed.\nlinkedinHandle: &linkedinHandle >-\n  return String(config.linkedinHandle || '').trim()\n    .split('/in/').slice(-1)[0]\n    .split('?')[0].split('#')[0]\n    .split('/').filter(Boolean).slice(-1)[0] || ''\n"
      }
    },
    "dir": "HunterEmailVerifier",
    "files": {
      "api/events.yaml": "# Hunter wraps everything in `data`. Each row reads through it, so a shape change shows up\n# as an empty field rather than a crash.\n\n- emit: email\n  from: response\n  value: \"return (response.data || {}).email || String(config.email || '').trim()\"\n\n- emit: status\n  from: response\n  value: \"return (response.data || {}).status || 'unknown'\"\n\n- emit: result\n  from: response\n  value: \"return (response.data || {}).result || ''\"\n\n- emit: score\n  from: response\n  value: \"return (response.data || {}).score || 0\"\n\n- emit: accept_all\n  from: response\n  value: \"return !!(response.data || {}).accept_all\"\n\n- emit: disposable\n  from: response\n  value: \"return !!(response.data || {}).disposable\"\n",
      "api/run.yaml": "- name: verify\n  method: GET\n  url: https://api.hunter.io/v2/email-verifier\n  credential: { $ref: hunter#queryCredential }\n  query:\n    email: \"return String(config.email || '').trim()\"\n  transport: json\n  error: { $ref: hunter#error }\n  retry:\n    attempts: 3\n    backoff: exponential\n    on: [429, 500, 502, 503, 504]\n",
      "config.yaml": "configSchema:\n  type: object\n  required: [email]\n  properties:\n    email:\n      type: string\n      title: Email\n      description: The address to verify\n      default: \"\"\n      \"ui:field\": template\n\"ui:order\": [email]\n",
      "interface.yaml": "inputs:\n  - name: signal\n    type: object\n    description: Trigger. The email usually templates from upstream output.\n\noutputs:\n  - name: email\n    type: string\n    description: The address that was verified\n  - name: status\n    type: string\n    description: valid, invalid, accept_all, webmail, disposable or unknown\n  - name: result\n    type: string\n    description: deliverable, undeliverable or risky\n  - name: score\n    type: number\n    description: Confidence 0-100\n  - name: accept_all\n    type: boolean\n    description: True when the domain accepts every address, which makes the result less certain\n  - name: disposable\n    type: boolean\n    description: True when the address is from a disposable provider\n\ncredentials:\n  - name: hunterCredential\n    required: true\n    displayName: Hunter.io\n",
      "node.yaml": "type: HunterEmailVerifier\nkind: PromiseNode\nname: Hunter Email Verifier\ncategory: Go To Market\ncolor: \"#FF5500\"\nlogoUrl: https://res.cloudinary.com/sonik/image/upload/v1781366295/gravity/icons/hunter.png\ndescription: Check whether one email address is real and deliverable\nwhenToUse: >\n  Pick to CHECK that a single email address is real and deliverable before sending. Use it\n  to verify an address you already have; discovering an unknown address is a separate,\n  upstream step.\n\n# WHO MAY RUN THIS (DECLARATIVE_NODES.md §9.13). Compulsory on every node, so that a node\n# nobody thought about cannot look the same as one deliberately left open.\n#\n# `required: false` adds no requirement of its own: the node runs for whoever the trigger\n# admitted. The trigger instance's publicEntry toggle still owns the door, and requirements\n# only ever narrow.\nauth:\n  required: false\n\ncapabilities:\n  cacheable: true\n\n# One vendor. The package allows Apollo too, for its other four nodes.\nallowedHosts: [api.hunter.io]\n",
      "test.yaml": "testData:\n  config:\n    email: steli@close.io\n  inputs:\n    signal: {}\n  expect:\n    email: \"return output.email.indexOf('@') > 0\"\n    status: \"return typeof output.status === 'string' && output.status.length > 0\"\n    score: \"return typeof output.score === 'number'\"\n"
    }
  }
}
